
<h5>
    {% set i = 0 %}

    {% for c in pathcategory %}

{#        La boucle pour pas avoir le point au debut#}
       {% if i == 0 %}
           <a href="{{ path('ressource.new') }}" class="btn btn-outline-secondary">Catégorie</a>

           {#           <button class="btn btn-outline-secondary">{{ c }}</button>#}
           <a href="{{ path('ressource.new', {categId: c.id}) }}" class="btn btn-outline-secondary">{{ c }}</a>
       {% else %}

           •    <a href="{{ path('ressource.new', {categId: c.id}) }}" class="btn btn-outline-secondary">{{ c }}</a>

           {#           <button class="btn btn-outline-secondary">{{ c }}</button>#}

       {% endif %}

       {% set i = i+1 %}
   {% endfor %}
</h5>
